# SEC-001 — Regressão de segurança do MVP

- [x] Contrato D06 §15, D07 §16 e backlog SEC-001 conferidos contra as rotas públicas de mutação.
- [x] Curtida e comentário recusam origem diferente, `Sec-Fetch-Site` diferente de `same-origin` e `Referer` divergente quando é o único sinal disponível.
- [x] Testes cobrem recusa antes de persistir e aceitação de proveniência válida; requisições legadas sem esses headers continuam aceitas.
- [x] Auditoria de dependências de produção executada; CI configurado para bloquear alertas altos e críticos.
- [x] Endpoint de comentários distingue mídia incompatível (`415`), JSON malformado (`400`), schema inválido e corpo acima de 10 KB, sem persistir, consumir rate limit ou registrar erro operacional.
- [ ] Avaliar e resolver ou aceitar formalmente os dois alertas moderados transitivos de `@clerk/ui` antes do gate final.
- [ ] Completar corpus de autorização negativa, XSS/Markdown, SQL injection, upload, payload, rate limit e exposição de segredo.
- [ ] Validar comportamento em Preview, WAF, resultado do scanning no CI e revisão final de exceções.

Estado: `in_progress`. A proteção de proveniência é parcial e não substitui a regressão SEC-001 completa.
